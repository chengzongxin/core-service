<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.czx.mapper.UserMapper">
    
    <resultMap id="UserResultMap" type="com.czx.pojo.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="is_active" property="isActive"/>
        <result column="is_admin" property="isAdmin"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>
    
    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="com.czx.pojo.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, password_hash, is_active, is_admin, created_at, updated_at)
        VALUES (#{username}, #{email}, #{passwordHash}, #{isActive}, #{isAdmin}, NOW(), NOW())
    </insert>
    
    <update id="update" parameterType="com.czx.pojo.User">
        UPDATE users 
        SET username = #{username},
            email = #{email},
            password_hash = #{passwordHash},
            is_active = #{isActive},
            is_admin = #{isAdmin},
            updated_at = NOW()
        WHERE id = #{id}
    </update>
    
</mapper>
